<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}StudentGaming{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
        <script src="https://kit.fontawesome.com/76fc774c21.js" crossorigin="anonymous"></script>
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100 justify-content-between">
        {% include "_partials/_header.html.twig" %}
        {% if app.user and app.user.isVerified == false %}
            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                <div class="alert-message">
                    <strong>Attention !</strong> Votre compte n'est pas encore vérifié, 
                    vous ne pouvez pas encore accéder à toutes les fonctionnalités du site. 
                    <a href="{{ path('resend_token') }}">Renvoyer le lien d'activation</a>
                </div>
            </div>
        {% endif %}
        {% include "_partials/_flash.html.twig" %}
        {# {% include "_partials/_sideBar_Menu.html.twig" %} #}
        {% block body %}
        {% endblock %}
        {% include "_partials/_footer.html.twig" %}
    </body>
</html>
